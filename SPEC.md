# 24DOST 기술·기능 명세서

macOS 도트 매트릭스 영상 플레이어. 영상을 도트 격자로 렌더링하고, 음성을 온디바이스로 인식·번역해 자막을 만든다.

| 항목 | 값 |
|---|---|
| 번들 ID | `com.dost.app` |
| 실행 파일 / 표시 이름 | `24DOST` |
| 최소 macOS | 26.0 |
| 저장소 | https://github.com/marzipan2025/24DOST |
| 서명 | ad-hoc (공증 없음) |
| 소스 규모 | Swift 9개 파일, 약 8,400줄 |
| 외부 의존 | ffmpeg (DMG에 임베드) |

> 이 문서는 코드에서 직접 확인한 사실만 담는다. 값이 바뀌면 문서도 함께 고쳐야 한다.

---

## 1. 계보

04DOPL(도트 매트릭스 플레이어)과 23VIWH(자동 자막 플레이어)를 합친 것이다. 04DOPL의 도트 렌더링과 사용성을 그대로 두고, 23VIWH에서 만든 자막 생성 파이프라인을 얹었다. 세 앱은 각각 독립적으로 유지된다.

---

## 2. 파일 구성

| 파일 | 역할 |
|---|---|
| `DostApp.swift` | 앱 진입점, 메뉴, 알림 이름, 최근 항목(Recents), 외부 열기 요청 |
| `ContentView.swift` | 도트 캔버스 렌더링, 키·제스처 입력, 자막 타이포그래피, 창 관리 |
| `VideoSampler.swift` | 재생 엔진, 프레임 샘플링, remux, 자막 모드 선택·표시 |
| `SubtitleGenerator.swift` | 자막 생성 파이프라인 (창 스케줄링, 커버리지, 캐시, 정교화) |
| `Transcriber.swift` | Apple SpeechAnalyzer 래퍼, 오디오 스트리밍, 세그먼트 분할 |
| `Translator.swift` | 번역 백엔드 두 가지 (Apple / Claude), Keychain 키 저장 |
| `SubtitleModels.swift` | `SubtitleCue`, 등급 정의, .srt/.smi 파서 및 생성기 |
| `SettingsWindowView.swift` | 설정 창 4개 탭, 소프트웨어 업데이터 |
| `OpenMediaURLIntent.swift` | App Intents — 외부에서 URL 재생 요청 |

### 2.1 창은 하나뿐이다 (중요)

재생 창은 **`WindowGroup` 이 아니라 `Window` 씬**이다. 이 앱은 창이 여럿인 걸 감당하지
못한다 — 키 입력을 받는 `NSEvent` 로컬 모니터가 창이 아니라 **앱 단위**라서 창마다 달린
모니터가 모든 키를 다 받고, 파일 열기도 알림 방송이라 모든 창이 같은 영상으로 갈아탄다.
최근 항목·재생 위치·자막 설정도 전부 앱 단위다.

`Window` 씬이라서 따라오는 두 가지 처리:

- 실행 중에 파일 열기 요청을 받으면 SwiftUI 가 창을 화면 밖으로 내린다(내용은 새 영상으로
  정상 전환된 상태). `AppDelegate.presentPlaybackWindow()` 로 다시 올린다.
- 그 순간을 "마지막 창이 닫혔다"고 보고 앱이 종료되면 안 되므로
  `applicationShouldTerminateAfterLastWindowClosed` 는 **false**. 대신 ⌘W(Close Window)가
  재생 창일 때 직접 `NSApp.terminate` 한다. 설정 창이 앞이면 설정 창만 닫는다.

나중에 생긴 창을 닫는 방식은 쓰지 말 것 — 닫으면 SwiftUI 가 즉시 되살려서 닫기/되살리기가
무한히 반복된다(0.0.5.13 작업 중 확인).

---

## 3. 재생

### 3.1 지원 형식

**네이티브 재생** — AVFoundation이 직접 여는 컨테이너.

**remux 후 재생** — ffmpeg로 MP4로 다시 담은 뒤 재생한다.
`mkv` `webm` `avi` `flv` `wmv` `ogv` `ogg` `wma` `rmvb` `rm` `ts` `m2ts` `mts` `vob` `asf` `divx` `xvid`

**오디오 전용** — 영상 없이 주파수 시각화로 재생.
`mp3` `aac` `m4a` `flac` `wav` `aiff` `aif`

**정적 이미지** — 한 번 샘플링해 도트로 표시.
`jpg` `jpeg` `png` `gif` `bmp` `tiff` `tif` `heic` `heif` `webp`

### 3.2 remux 규칙

코덱을 먼저 조사해 **재인코딩 없이 담을 수 있으면 stream copy**한다.

- 비디오 복사 가능: `h264` `hevc` `mpeg4` `mjpeg` `prores` — 아니면 libx264 재인코딩(veryfast, crf 23)
- 오디오 복사 가능: `aac` `mp3` `ac3` `alac` `eac3` — 아니면 AAC 192k 재인코딩
- 자막: 텍스트 기반(`subrip` `srt` `ass` `ssa` `mov_text` `text` `webvtt`)만 `mov_text`로 변환. **비트맵 자막(PGS/VobSub)은 드롭한다** — mov_text로 지정하면 remux 전체가 실패해 BluRay 립 mkv가 통째로 안 열렸다.
- HEVC를 stream copy할 때 `hvc1` 태그를 강제한다. ffmpeg 기본값 `hev1`은 AVFoundation이 못 읽는다.

### 3.3 도트 렌더링

프레임을 CVPixelBuffer(BGRA)로 받아 격자 단위로 평균색을 뽑아 원으로 그린다.

| 값 | 기본 | 제약 |
|---|---|---|
| 격자 간격 `gridSize` | 40 | ≥ 도트 지름 + 1 |
| 도트 지름 `dotDiameter` | 16 | ≥ 8 |

**격자는 창 전체를 채운다(창 모드).** 창 비율에 맞는 중앙 영역만 원본에서 읽으므로 가장자리는 잘린다 — 피크가 영상을 채우는 방식(`.resizeAspectFill`)과 같아서 두 모드가 같은 화면을 보여주고, 자막 앵커도 창 하단에 붙는다. 늘리는 방식은 쓰면 안 된다: 점 하나가 그 위치의 색을 가져오는 구조라 점을 촘촘히 할수록 늘어난 그림이 그대로 드러난다.

**전체화면은 비율을 지킨다.** 거기서는 피크가 `.resizeAspect`(레터박스)로 바뀌고, ⌘0(fit)/⌘1(fill)/핀치 줌이 전부 "fit = 1.0"을 기준으로 정의돼 있어 창 모드처럼 처리하면 fit과 fill이 구분되지 않는다.

샘플링은 stride 대신 정규화 매핑을 쓴다. `max(1.0, srcH/rows)` 방식은 행 수가 원본 세로 픽셀 수를 넘는 순간(세로로 긴 창 + 좁은 간격) 아래쪽 행이 전부 마지막 스캔라인을 읽어 화면 아래가 뭉개진다.

창 모서리 안쪽 30pt 영역은 라운드 마스크로 도트를 지운다. 간격이 30 미만이면 마스크가 활성화되고, 그때 가장 바깥 줄이 마스크에 걸린다 — 재생/일시정지 테두리를 고정 행·열 번호로 잡으면 안 되는 이유다(§6).

---

## 4. 자막 표시

### 4.1 자막 소스 네 가지

`off` → `external`(사이드카 파일) → `embedded`(컨테이너 내장) → `generated`(직접 생성). **P** 키로 순환하며, 존재하는 것만 순환에 들어간다.

- **external** — 영상과 **정확히 같은 이름**의 `.srt`/`.smi`. 우선순위는 srt > smi. 발견하면 사용할지 화면에서 묻는다(Enter = 사용, Esc = 무시).
- **embedded** — AVFoundation의 legible 미디어 선택 그룹. 강제 자막(forced)은 제외.
- **generated** — §5 참조.

### 4.2 한글 타이포그래피

내장 도트 폰트(BPdots Unicase)에는 한글 글리프가 없다. 한 글자라도 한글이 섞이면 **코레일체**로 그린다.

- 배율 **0.75** — BPdots 대비. 잉크 높이를 실측하면 0.543에서 기하학적으로 일치하지만(100pt에서 BPdots "HELLO" 47.5 / 코레일 "한글자막" 87.4), 그 값은 눈으로 보면 한글이 작고 답답하다. 기하학적 일치의 1.38배로 잡았다.
- 웨이트 — 30.5pt 이상 Light, 21.75pt 이상 Medium, 그 아래 Bold. 작을수록 굵게 가야 뭉개지지 않는다.
- **줄 상자 높이는 배율과 무관하게 항상 기준 크기**다. 이 높이가 글자 뒤 도트를 비우는 영역이라, 글자 크기에 비례시키면 작은 한글 자막에서 위아래 도트가 글자에 바짝 붙는다.
- 베이스라인은 두 폰트의 ascender 차이만큼 내려 맞춘다. 안 그러면 자막 소스를 오갈 때 글자가 위아래로 튄다.

측정과 렌더가 **반드시 같은 폰트**를 써야 한다. 어긋나면 글자 뒤 도트가 남거나 과하게 지워진다. 그래서 폰트 선택은 `subtitleTypeface(for:baseSize:)` 한 곳만 통한다.

### 4.3 한글 정규화

화면에 나가는 모든 텍스트는 **완성형(NFC)으로 정규화**한다.

APFS는 파일명을 **자모 분해(NFD)** 로 저장한다. "시간제"가 파일명에서는 `U+1109 U+1175 U+1100 U+1161 U+11AB U+110C U+1166` 일곱 개다. **코레일체에는 조합용 자모(U+1100–11FF) 글리프가 없어서** 시스템 폴백으로 넘어가고, 그 폰트는 자모를 합치지 않고 낱자로 그린다 — ⌘I 파일명이 `ㅅㅣㄱㅏㄴㅈㅔ` 로 흩어져 보였던 원인이다.

폭 측정과 렌더가 같은 문자열을 써야 하므로 **텍스트가 두 갈래로 나뉘기 전 한 곳**(`draw()` 안, `overlayRawText` 확정 직후)에서 정규화한다. 이미 완성형인 문자열에는 영향이 없다.

---

## 5. 자막 생성 파이프라인

핵심 아이디어는 **실시간이 아니라 재생 위치보다 앞서 달리는 것**이다. 파일 재생에서는 이게 실시간보다 쉽고 결과도 낫다 — 자막이 타임스탬프에 맞춰 정확히 뜨므로 사용자 눈에는 지연이 0이다.

```
오디오 구간 → SpeechAnalyzer 인식 → 세그먼트 분할 → 번역 → 큐 삽입 → 커버리지 기록 → 캐시
```

### 5.1 인식 (Transcriber.swift)

macOS 26 `SpeechAnalyzer` + `SpeechTranscriber`. 완전 온디바이스, 오디오는 외부로 나가지 않는다.

- 오디오는 임시 파일로 떨구지 않고 `AVAssetReader`에서 바로 PCM 버퍼로 흘려보낸다.
- 분석기는 **같은 구간을 여러 번 다듬어 내보낸다.** 그대로 이어붙이면 같은 문장이 서너 번 반복된다. 시작 시각을 키로 쓰는 방식은 통하지 않는다(다듬을 때 타임스탬프가 5.200 → 6.300처럼 미세하게 움직인다). 그래서 **시간축을 한 방향으로만 소비한다** — 이미 지나온 지점보다 앞서 시작하는 run만 받는다.
- **세그먼트 분할은 시계가 아니라 말의 쉼으로 정한다.** 예전에는 6초가 되면 그 자리에서 무조건 끊었는데, 일본어처럼 띄어쓰기가 없는 언어는 Apple 이 run 을 단어보다 작게 쪼개 주기 때문에 절단선이 단어 한가운데 걸렸다("はいプ" / "ライベートでは"). 실측한 3분 구간에서 35개 중 14개가 정확히 6.00초짜리 — 전부 상한에 잘린 것이었다. 순서는:
  1. 문장부호(`。？！`) — 바로 끊는다
  2. **soft 6초**를 넘겼고 다음 run 까지 쉼이 있다 — 거기서 끊는다. 요구하는 쉼 길이는 hard 에 가까워질수록 짧아진다
  3. **hard 12초** 또는 84자 — 지금 자리가 아니라 모아 둔 구간 안에서 **가장 크게 쉰 자리로 되돌아가** 끊는다
- 6초라는 값 자체는 타당하다. 같은 구간을 VAD 로 나눠 보면 자연스러운 발화 한 덩어리는 중앙값 1.6초, 90%가 3.8초 이내, 6초를 넘는 건 3%뿐이다.
- 표시 시간 제한(한 자막이 화면에 떠 있는 시간)은 **끊는 지점과 별개**다. 누적 중이 아니라 확정할 때 건다 — 누적 중에 잘라 두면 절단을 미루는 동안 뒤 텍스트가 이미 끝난 큐에 계속 달라붙는다.
- 앞 자막이 다음 자막 시작을 넘어 살아 있으면 잘라 준다(두 줄이 겹쳐 보이는 것 방지).
- 큐 배열 전체의 불변식(**겹치지 않고 같은 대사가 두 벌 남지 않는다**)은 `SubtitleGenerator.normalizeCues()` 가 삽입 경로 끝에서 강제한다. 삽입 지점마다 따로 막으려다 계속 구멍이 났다 — 특히 정교화는 지우는 기준이 시작 시각이라, 대상 구간 앞에서 시작해 안쪽까지 걸친 큐가 살아남아 겹쳤다.

### 5.2 번역 (Translator.swift)

| 백엔드 | 특성 |
|---|---|
| **Apple** (기본) | 온디바이스, 무료, 오프라인. **줄 단위 독립 번역** — 앞뒤 문맥을 보지 않는다. |
| **Claude** | API 키 필요(Keychain 저장, 서비스명 `com.dost.app`). 창 하나 분량을 한 번에 보내고 앞 대사 3줄을 문맥으로 준다. 존댓말·인물 지칭·말투가 줄 사이에 이어진다. |

Claude 쪽 주의:
- 시스템 프롬프트에 프롬프트 캐시(`cache_control: ephemeral`)를 건다. 캐시가 살려면 이 문자열이 **바이트 단위로 동일**해야 한다.
- Sonnet 5 / Opus 5는 확장 사고가 기본이라 `thinking: disabled`로 끈다. Haiku 4.5는 파라미터를 생략하면 사고 없이 동작한다.
- **키를 저장하지 않고 Claude를 고르면 조용히 실패한다** — 번역기를 못 만들면 원문을 그대로 자막으로 쓴다. 오류 표시가 없다.
- 인식이 뭉개진 구간은 Apple 번역도 파싱을 못 해 **원문을 그대로 통과시킨다.** 영어가 그대로 나오는 건 대개 번역 설정 문제가 아니라 인식 실패다.
- 서버로 텍스트가 전송된다. 사용 정책이 적용되므로 성적으로 노골적인 대사는 거절될 수 있고, 거절 답변은 줄 수가 안 맞아 원문으로 폴백된다.

번역 소요는 **구간에 따라 4~13초**까지 걸린다. 첫 실행이나 콜드 상태에서 특히 느리다.

### 5.3 창 스케줄링

창마다 SpeechAnalyzer를 **새로 만든다.** 따라서 창 경계는 전부 콜드 스타트다 — 앞 문맥 없이 다시 시작하니 경계 근처 인식이 가장 나쁘고, 경계에 걸친 세그먼트는 잘렸을 가능성 때문에 버려진다. **창은 길수록 좋다.**

그런데 창이 길면 그 창이 끝나야 자막이 나오므로 사용자가 기다리는 자리에서는 손해다. 그래서 셋으로 나눈다.

| 등급 | 창 길이 | 언제 |
|---|---|---|
| 0 `urgent` | 60초 | 재생 위치에 준비된 게 없어 기다리는 중 |
| 1 `relaxed` | 180초 | 재생 위치보다 앞서 달리는 중 |
| 2 `fine` | 300초 | 만들 게 다 떨어져 다시 만드는 중 |

- **급한지 판정**: `frontier >= playhead - 5 && frontier <= playhead + fastResponseRange`.
  "look-ahead 안쪽"만 보면 안 된다 — 백필(재생 위치보다 **뒤쪽** 빈 구간 메우기)은 frontier가 playhead보다 작아서 그 조건을 항상 통과한다. 제일 안 급한 작업이 전부 짧은 창으로 처리된다. 실제로 그렇게 짜 놓고 재 보니 전체의 70%가 urgent로 분류됐고, 고친 뒤 11.6%가 됐다.
- 창 앞에 **2.5초 겹침**을 덧댄다. 분석기가 달리기 시작할 구간을 줘서 경계 첫 마디가 잘리는 걸 막는다. 이 구간의 세그먼트는 경계 필터가 걷어낸다.
- 창 끝에 딱 붙어 끝나는 세그먼트는 말이 잘렸을 가능성이 커서 버리고, 다음 창을 그 세그먼트 시작점부터 다시 읽는다.
- **Fast Response Range**(설정, 기본 40초 / 3·10·20·40·80·100초)는 위 판정에만 쓴다. 이 범위 안쪽은 빠른 응답을 우선해 짧은 창을, 바깥은 품질을 우선해 긴 창을 쓴다.
  **넓힐수록 품질이 나빠진다.** 짧은 창으로 만드는 구간이 늘어나기 때문이다. 예전 이름 `Look Ahead`("이만큼 앞서면 멈춘다")는 전체 미리 생성을 넣으면서 뜻을 잃었는데 이름만 남아, 값을 올리면 더 많이 준비된다고 오해하기 쉬웠다. 0.0.4.9에서 이름과 설명을 실제 동작에 맞췄다.
- 생성은 이 값과 무관하게 **파일 끝까지 계속된다.** 재생 위치 이후가 우선이고, 끝까지 다 차면 앞으로 돌아가 건너뛴 구간을 메운다. 실측 실시간의 약 3.5배 속도라 2시간 영화가 30분 남짓이면 전부 찬다.

성능 실측(62초 창 / 171초 창):

| | 인식 중앙값 | 최대 | 실시간 대비 |
|---|---|---|---|
| 62초 창 | 0.20초 | 2.3초 | 312배 |
| 171초 창 | 0.60초 | 0.8초 | 285배 |

창을 2.7배 늘려도 처리 시간은 비례해서만 늘어난다. 긴 입력에서 비선형으로 무너지지 않는다.

### 5.4 정교화 (볼수록 좋아지는 자막)

탐색할 때마다 60초짜리 성긴 구간이 하나씩 생긴다. 그대로 두면 많이 볼수록 성긴 구간이 쌓이기만 한다. 그래서 **만들 게 다 떨어지면 낮은 등급 구간을 긴 창으로 다시 만든다.**

- 등급은 `coveredRanges`에 섞지 않고 **`pendingRefine`이라는 별도 목록**으로 둔다. 커버리지 병합 규칙은 탐색 정확성의 핵심이라 건드리지 않는다.
- **한 칸씩만 올린다** (0→1→2). 곧바로 최고 등급으로 보내면 연산은 아끼지만 단계별 결과를 비교할 수 없다.
- 대기열은 등급 낮은 순으로 정렬 — 60초로 급조한 구간부터 고친다.
- 정교화 창은 **대상 구간보다 60초 앞에서 연다.** 대상 자리에서 창을 시작하면 거기가 다시 콜드 스타트라 의미가 없다.
- 이 경로에서는 **"이미 덮인 구간에서 창을 끊는" 처리를 하지 않는다.** 정교화는 정의상 이미 만든 데를 다시 읽는 일이라, 끊으면 창이 도로 짧아져 아무것도 안 나아진다.
- 채택은 **대상 구간 안쪽 큐만** 한다.
- 대기열이 비면 멈춘다. 영원히 도는 일은 없다.

**나빠질 수 없다는 보장** — 다시 만든 결과가 비었거나 **자막 수 또는 글자 수 중 하나라도 줄면 기각**하고 옛것을 유지한다. 개수만 보던 시절 `큐 6→6, 글자 65→60`인 결과가 통과한 적이 있어 글자 수 검사를 더했다. 채택 여부와 무관하게 대상은 목록에서 뺀다 — 안 그러면 같은 구간을 계속 붙든다.

> **미검증 가설**: "창이 길수록 품질이 좋다"는 아직 수치로 증명되지 않았다. 30초 대 60초 비교에서 8% 더 잡은 것이 유일한 방향 근거이고, 정교화 실측에서는 기각 사례가 여러 번 나왔다. 긴 창이 항상 이기지는 않는다.

### 5.5 진단용 색 표시

**Adaptive Subtitle Color를 끄면** 자막을 만든 창 등급이 색으로 드러난다. **생성 자막에만 적용된다** — 외부·내장 자막은 우리가 만든 게 아니라 등급이라는 개념이 없으므로 평소 색으로 그린다(`currentSubtitleTier`가 nil).

| 색 | 등급 |
|---|---|
| 평소 색 | 60초 |
| 악센트 색 | 180초 |
| 형광 녹색 `#38FF59` | 300초 |

### 5.6 커버리지

"어디까지 만들었다"를 **숫자 하나로 들 수 없다.** 사용자가 타임라인을 뛰어다니면 커버리지가 조각나기 때문이다. 스칼라로 들면 뒤로 되감았을 때 그 값이 남아 "이미 다 만들었다"고 판단해 버린다. 그래서 **구간 집합**으로 관리하고, 0.5초 이내로 붙은 것은 병합한다. 1초 미만의 틈은 덮인 것으로 친다.

**빈 인식 결과는 영구 기록하지 않는다.** 말이 하나도 안 나온 창은 진짜 무음일 수도, 디코딩이 잠깐 어긋난 것일 수도 있는데 구분할 방법이 없다. 커버리지를 아예 안 남기면 같은 창을 무한히 다시 읽으므로, **세션 안에서는 덮인 것으로 쳐서 전진하되 캐시에는 그 구간을 도려내고 쓴다.** 파일을 다시 열면 한 번 더 시도한다.

### 5.7 취소 처리 — 중요

탐색이 감지되면(두 tick 사이 시각이 **2초** 넘게 튀면) 진행 중이던 작업을 취소한다. 그런데 **인식이 끝난 뒤에는 취소를 확인하지 않는다.**

인식이 끝난 순간 그 결과는 재생 위치와 무관하게 유효한 데이터다. 번역이 4~13초 걸리는 동안 사용자가 한 번만 움직여도 그 창이 통째로 날아가면, 자주 탐색하는 사용자에게는 커버리지가 0에서 꿈쩍도 안 한다. (v0.0.4.6 이하에서 "자막이 아예 안 나온다"로 나타난 실제 버그다.)

밀려난 작업인지는 **세대 번호**(`jobGeneration`)로 가린다. 결과는 저장하되 다음 창을 고르는 흐름만 건드리지 않는다. 세대 번호가 필요한 이유는 취소가 협조적이기 때문이다 — 취소를 알아채지 못한 옛 작업이 완료 블록까지 도달해 `currentJob`을 덮으면, 그 사이 시작된 새 작업의 핸들이 사라져 추적이 끊기고 작업이 둘로 늘어난다.

Apple 번역기는 `withCheckedContinuation`이라 취소를 관찰하지 않으므로 중간에 끊기지 않는다. (Claude 백엔드는 URLSession이라 취소에 반응한다.)

### 5.8 캐시

경로: `~/Library/Application Support/24dost/subtitles/<해시>.<인식언어>_<대상언어>.<엔진>.json`
해시 = SHA256(`파일 URL` + `|` + `파일 크기`)의 앞 20자.

SRT가 아니라 JSON인 이유는 **"어디까지 만들었는지"를 담아야** 하기 때문이다. 큐의 시각에서 역산하면, 말이 없어서 큐가 안 생긴 구간을 "아직 안 만든 곳"으로 오해한다.

```json
{
  "version": 4,
  "covered": [[0, 60], [154, 446]],
  "pending": [[0, 60, 0]],
  "cues": [{ "start": 1.2, "end": 4.0, "text": "…", "sourceText": "…", "tier": 1 }]
}
```

- 형식 버전이 다르면 무시하고 다시 만든다. 품질에 영향을 주는 변경이 있을 때 버전을 올린다.
- **인식 결과는 도착어·엔진이 달라도 재사용한다.** 인식기에는 도착어가 전달되지 않으므로(`transcribe` 의 locale 은 출발어뿐) 같은 오디오·같은 출발어면 원문이 글자 하나까지 같다. 새 조합으로 바꾸면 인식 언어가 같은 캐시 중 커버리지가 가장 넓은 것을 골라 그 `sourceText` 를 세그먼트로 꺼내 쓰고, 번역만 새로 한다. **정교화 경로는 제외한다** — 그건 "더 긴 창으로 다시 듣는 것"이 목적이라 재사용하면 의미가 없다. 재사용 세그먼트에는 창 끝 폐기 휴리스틱도 적용하지 않는다(이미 제대로 잘려 저장된 것이라 멀쩡한 대사를 잃는다).
- **인식 언어·대상 언어·엔진별로 파일이 갈린다.** 인식 언어는 해석된 실제 로케일(지역 포함 — en-GB 와 en-US 는 결과가 다르다)을 쓴다. 0.0.5.4 이전에는 인식 언어가 키에 없어서, 언어를 잘못 잡고 만든 자막이 설정을 고쳐도 같은 파일에서 다시 읽혀 남아 있었다.
- **엔진·언어별로 파일이 갈린다.** 따라서 번역 엔진이나 대상 언어를 바꾸면 인식부터 전부 다시 한다. (인식 결과는 언어·엔진과 무관하므로 캐시를 둘로 쪼개면 재사용할 수 있다 — 미구현.)
- 엔진·언어·모델이 바뀌면 생성기를 **새 조합으로 다시 붙인다.** 안 하면 커버리지가 "다 만듦"이라 재생성도 안 되고 옛 설정으로 만든 자막이 계속 보인다.

**Clear Subtitle Cache** (설정 → General) — 이 맥에 저장된 생성 자막을 전부 지우고 현재 영상 것부터 다시 만든다. 설정은 건드리지 않는다.

### 5.9 이 영상 데이터 삭제 (⌘X)

이 영상에 대해 앱이 기록한 것을 지운다. **되돌릴 수 없으므로 반드시 한 번 묻는다**
(도트 프롬프트 "DELETE DATA?" → `X` / `DELETE`). 지운 뒤에는 지운 파일 수를 알린다.

지우는 것:

- 생성 자막 캐시 — **언어·엔진 조합을 가리지 않고 전부**. 파일명이 `<hash>.<src>_<dst>.<engine>.json`
  이라 해시로 시작하는 것을 모두 걷어낸다.
- 저장된 재생 위치
- 이 영상에 대해 기억해 둔 인식 언어

**최근 항목은 건드리지 않는다** — 캐시가 아니라 무엇을 봤는지의 기록이고, 지금 재생 중인
파일이라 지워도 곧바로 다시 들어간다.

지운 다음에 다시 쌓을지는 **설정이 정한다** — 파일을 처음 열 때와 같은 규칙이다.
Generate When No Subtitles Exist 가 켜져 있으면 곧바로 다시 만들기 시작하고, 꺼져 있으면
사용자가 `p` 로 auto sub 에 들어올 때부터 만든다. 그래서 생성기는 `regenerate()` 가 아니라
`discardAll()` 로 비운다 — 지우자마자 되살아나면 지운 게 아니다.

### 5.10 내보내기

**⇧⌘E** — 영상과 같은 폴더에 `<영상명>.<언어>.autosub.srt` 로 저장. 확장자 바로 앞에 `.autosub`를 붙여 사람이 만든 자막과 구분하되, 파일 목록에서 영상 옆에 나란히 정렬되게 했다. 사이드카 자동 인식은 영상 파일명과 정확히 같은 이름만 잡으므로 영향 없다.

---

## 6. Peek 모드

우상단 도트 한 칸을 누르면 도트를 걷어내고 실제 영상을 보여준다. **끝나는 건 커서가 그 칸을 벗어날 때**다 — 버튼을 계속 누르고 있을 필요가 없다. **Tap to Peek** 설정을 켜면 누를 때마다 토글된다.

> 롤아웃 하나에만 맡기면 갇힌다. 전체화면에서는 커서가 2초 뒤 자동으로 숨는데, 숨은 채 도트 위에 있으면 hover 가 빠지지 않는다. 그래서 **커서가 이미 밖에 있는 상태의 릴리즈**와 **창 비활성화**도 종료 조건으로 둔다.

> **창 비활성화로 끝내는 건 롤아웃 모드에서만이다.** Tap to Peek 은 눌러서 켜고 다시 눌러 끄는 명시적 토글이라 커서 위치에 의존하지 않는다. 거기서도 같이 끝내면 다른 앱을 잠깐 보고 돌아왔을 때 멋대로 도트로 돌아가 있다. (영상이 바뀌면 — 재생이 끝나 다음 항목으로 넘어가는 경우 포함 — 모드와 무관하게 끝난다.)

- 도트를 통째로 건너뛰는 방식이라, 볼륨·탐색 직선도 같이 지워졌었다. 지금은 **오버레이 직선에 해당하는 도트는 피크 중에도 그린다.** 글자 영역은 도트 모드와 같은 검사를 거쳐, 재생/일시정지 테두리가 좌하단 자막 위에 겹치지 않는다.
- 우클릭 세로줄 탐색과 스크롤 볼륨도 피크 중에 동작한다. 우상단 도트는 피크 토글이라 탐색에서 제외된다.
- 깜빡임이 켜진 동안에는 우상단 앵커도 같은 색으로 간다. 깜빡임이 꺼진 위상에서는 앵커만 남는다 — 피크를 되돌릴 유일한 표적이라 사라지면 안 된다.

**재생/일시정지 테두리**는 "행 1, 열 1, 마지막 행, 마지막 열"이 아니라 **실제로 보이는 영역의 바깥 테두리**를 찾아 그린다. 고정 번호로 잡으면 도트 간격을 30 아래로 좁혔을 때 그 줄들이 모서리 마스크 안쪽에 들어가 통째로 사라진다.

---

## 7. 입력

### 7.1 일반

| 입력 | 동작 |
|---|---|
| ⌘O | 파일 열기 |
| ⌘U | URL 열기 |
| ⌘I | 재생 정보 |
| ⌘P | 자막 파일 열기 |
| ⌘E | 도트 이미지 PNG 내보내기 |
| ⇧⌘E | 생성 자막 .srt 내보내기 |
| ⌘X | 이 영상 데이터 삭제 (확인 후) |
| ⌘W | 창 닫기 |
| ⌘, | 설정 |

### 7.2 재생

| 입력 | 동작 |
|---|---|
| Space | 재생/일시정지, 대기 상태면 마지막 미디어 복원 |
| Return | 전체화면 토글 |
| ← / → | 10초 뒤로/앞으로 |
| ⇧← / ⇧→ | 이전/다음 파일 |
| , / . | 타임라인 한 칸 이동 |
| 0~9 | 0%~90% 지점으로 점프 |
| ↑ / ↓ | 볼륨 (최대 120%) |
| W / S | 도트 크기 |
| A / D | 도트 간격 |
| Z | 도트 설정 초기화 |
| B | 배경 스타일 순환 |
| T | 항상 위 토글 (전체화면 아닐 때만) |
| P | 자막 순환 |
| [ / ] | 자막 크기 |
| ⌘0 | 영상 크기에 맞춰 창 조정 |
| ⌘- / ⌘= | 창 축소/확대 |

### 7.3 제스처

| 입력 | 동작 |
|---|---|
| 파일 드롭 | 열기 또는 교체 |
| 더블 클릭 | 전체화면 |
| 싱글 클릭 | 재생/일시정지 |
| 도트 우클릭 | 클릭한 세로줄 위치로 점프 |
| 스크롤 | 볼륨 |
| 전체화면 드래그 | 도트 크기·간격 |
| 우상단 도트 | 피크 |
| 핀치 (전체화면) | 콘텐츠 줌 |

### 7.4 키 처리 주의

- 키 모니터는 `NSEvent.addLocalMonitorForEvents`. URL 편집 모드에서는 재생 단축키를 전부 무시하고 텍스트 입력만 받는다.
- **한글 입력기 상태에서는 `charactersIgnoringModifiers`가 "ㅍ" 같은 타 언어 문자를 반환한다.** ⌘V는 하드웨어 키코드 9를 함께 검사해 어떤 환경에서도 붙여넣기가 되게 했다.
- 자동 반복을 막아야 하는 키가 있다: `[49, 36, 76, 53, 17, 11, 35, 6]` (space, Return, Enter, Esc, T, B, P, Z).

---

## 8. 설정

### General
- Remember Playback Position / Playback Loop / Tap to Peek / Auto-resize Window to Video / Prevent Display Sleep in Fullscreen
- Accent Color
- Software Update
- **Clear Subtitle Cache** — 생성 자막만 삭제 (2단계 확인)
- **Reset Everything** — 설정·기록·**생성 자막 캐시**·앱 상태 전부 삭제 (2단계 확인). 캐시는 UserDefaults 밖(Application Support)에 있어서 키 삭제 루프로는 안 지워진다 — 0.0.5.6 이전에는 설명 문구와 달리 남아 있었다.

### Subtitles
> **다른 언어 캐시 프롬프트는 인식 언어가 다를 때만 뜬다.** 도착어만 다른 것(ja→ko 를 만들어 두고 ja→de 로 바꾼 경우)은 사용자가 의도한 정상 동작이므로 묻지 않는다. 이 프롬프트는 "인식 언어를 잘못 잡았다"를 잡으려는 것이다.

> **그리고 "아직 아무 선택도 안 한 영상"에서만 뜬다.** 이 영상에 대해 언어를 고른 적이 있으면(기억이 있으면) 묻지 않는다 — 안 그러면 설정에서 방금 일본어로 바꾼 직후에 "USE ENGLISH?" 가 떠서 방금 내린 결정을 되묻게 된다.

> **Spoken Language 에 Auto 는 없다.** 예전에 있던 Auto 는 감지가 아니라 시스템 선호 언어의 첫 번째를 쓰는 것이었다 — 한국어 맥에서 일본어 영상을 틀면 한국어로 인식을 시도해 쓰레기 자막이 나왔다. 이름이 하는 일과 달라 오해를 부르므로 없앴다. 목록도 지역 변종을 추려 13개로 줄였다(중국어는 Cantonese/Mandarin, 영어는 UK/US, 포르투갈어는 브라질/포르투갈, 스페인어는 스페인/멕시코, 나머지는 본토 하나씩).

- **Display** — Adaptive Subtitle Color, Subtitle Backdrop While Peeking
- **Automatic Subtitles** — Generate When No Subtitles Exist, Spoken Language, Translate Into, Fast Response Range(3/10/20/40/80/100초, 기본 40)
- **Translation Engine** — Engine(Apple/Claude), Model, API Key

### Shortcuts / Licences
표 형태의 읽기 전용 목록.

### 저장 키

| 키 | 내용 |
|---|---|
| `24dost.subtitle.*` | autoGenerate, sourceLanguage, targetLanguage, backend, claudeModel, lookAhead(= Fast Response Range, 키 이름은 옛 이름 유지) |
| `24dost.accentColor`, `24dost.backgroundStyle`, `24dost.fullscreenBackgroundStyle` | 외형 |
| `24dost.lastMedia.*` | kind, value, paths, title |
| `24dost.recents.v1` | 최근 항목 (최대 10, LRU) |
| `24dost.playbackPositions.v1` | 파일별 재생 위치 |
| `dost.gridSize`, `dost.dotDiameter`, `dost.subtitleFontSize`, `dost.volume` | 렌더·재생 |
| `adaptiveSubtitleColor`, `subtitleBackdropWhilePeeking`, `tapToPeek`, `rememberPlaybackPosition`, `loopMultiFilePlayback`, `preventFullscreenDisplaySleep`, `autoResizeWindowToVideo` | 토글 |

> UserDefaults 키와 URL 스킴은 제품명을 대문자화한 뒤에도 **소문자 `24dost`를 유지한다.** 바꾸면 기존 설정과 캐시를 잃는다.

---

## 9. 소프트웨어 업데이트

GitHub Releases의 `releases/latest`를 인증 없이 조회한다(공개 저장소여야 한다). 버전 비교는 `.`으로 나눠 부족한 자리를 0으로 채우므로 `0.0.4.1` 같은 4자리도 정상 동작한다.

확인은 **앱을 켤 때 한 번만** 한다. 새 버전이 있으면 대기 화면의 `24Dost` 자리가 `Update »` 로 바뀌고, 그 글자나 화살표를 누르면 설정 창의 Software Update 섹션이 열린다.

> **대기 화면에서만 보인다.** 영상을 열어 둔 상태에서는 어디에도 표시되지 않는다. 켜자마자 파일을 열면 안내를 볼 기회가 없다. 의도된 동작이다 — 업데이트가 항상 급한 것은 아니므로 켤 때 한 번 알리는 것으로 충분하다고 봤다.

흐름: `idle → checking → available → downloading → readyToInstall → (앱 종료) → 교체 → 재실행`

설치는 분리된 bash 헬퍼가 한다. 앱이 종료되길 기다렸다가 `ditto`로 스테이징하고, quarantine을 떼고, 원자적으로 교체한 뒤 재실행한다. 실패하면 백업을 되돌린다.

---

## 10. 빌드와 배포

```bash
xcodebuild -project Dost.xcodeproj -scheme Dost -configuration Release build
bash Packaging/make_dmg.sh
gh release create v<버전> Releases/<버전>/24DOST-<버전>.dmg --title "24DOST <버전>"
```

`make_dmg.sh`가 하는 일: Release 빌드 → 스테이징 → **ffmpeg/ffprobe와 의존 dylib 임베드**(rpath 재작성 + 재서명) → 배경 이미지와 아이콘 배치 → 압축 DMG.

- 버전은 `project.pbxproj`의 `MARKETING_VERSION`과 `make_dmg.sh`의 `VERSION`/`VOL_NAME` **양쪽**을 고쳐야 한다.
- DMG 배경은 `Packaging/24DOST_wallpaper.png`, **1280×960 @144dpi**(논리 640×480 = 창 크기). 72dpi로 만들면 1280×960 포인트로 해석돼 창보다 크게 나온다.
- Finder 아이콘 라벨은 `CFBundleName`이 아니라 **`CFBundleDisplayName`**을 쓴다. Info.plist를 직접 관리하는 프로젝트라 asset catalog만으로는 안 되고 `CFBundleIconName`도 직접 넣어야 한다.

---

## 11. 디버깅

환경변수 `DOST_DEBUG_LOG=1`로 실행하면 `/tmp/dost-debug.log`에 파이프라인 추적이 남는다.

```bash
open -a /Applications/24DOST.app --env DOST_DEBUG_LOG=1 /path/to/video.mp4
```

주요 로그:

| 문자열 | 의미 |
|---|---|
| `attach url=… cached=N` | 미디어 연결, 캐시에서 복원한 큐 수 |
| `applySubtitleMode: requested=… effective=…` | 자막 모드 결정 |
| `pump: window A-Bs (urgent\|relaxed\|refine) frontier=… ranges=N` | 창 선택 |
| `transcribed N segments` | 인식 완료 |
| `+N cues` | 큐 삽입 완료 |
| `refine A-Bs tierX→Y 채택\|기각 (큐 …, 글자 …)` | 정교화 판정 |
| `stale job gen=N` | 밀려난 작업 (결과는 저장됨) |
| `window FAILED` | 창 실패 |

**`transcribed`는 나오는데 `+N cues`가 없다** → 인식 후 단계(번역)에서 결과가 유실되고 있다. `ranges=`가 자라지 않는지 함께 본다.

os.Logger를 쓰지 않는 이유는 info 레벨이 기본으로 안 남고 프로세스 이름 매칭도 까다로워서다.

---

## 12. 테스트할 때 지켜야 할 것

이 세션에서 실제로 진단을 그르쳤던 것들이다.

1. **옛 인스턴스를 반드시 종료한다.** 여러 개 떠 있으면 로그가 섞여 엉뚱한 결론이 나온다.
   ```bash
   osascript -e 'quit app "24DOST"'; pkill -9 -f "24DOST.app/Contents/MacOS"
   ```
2. **조작을 섞어서 시험한다.** 가만히 두고 보는 테스트는 실사용을 재현하지 못한다. 탐색 중 자막이 유실되던 버그가 정확히 그래서 릴리즈까지 나갔다.
3. **캐시 상태를 확인한다.** 형식 버전이 바뀌면 옛 캐시는 무시된다. 성능을 재려면 캐시를 비우고 시작한다.
4. **번역 시간은 콜드/웜 차이가 크다.** 웜 상태의 0.6초를 보고 판단하면 실사용(4~13초)을 놓친다.
5. **개선을 주장하기 전에 같은 구간을 비교한다.** 서로 다른 장면을 비교하면 장면 난이도 차이를 개선으로 착각한다.

---

## 13. 알려진 한계와 미해결

- **창 길이와 품질의 상관은 미검증.** §5.4 참조.
- **캐시가 인식/번역으로 분리돼 있지 않다.** 언어나 엔진을 바꾸면 인식부터 다시 한다.
- **정교화가 기각되면 재시도하지 않는다.** 무한 루프를 막기 위한 선택이라, 일부 구간은 낮은 등급으로 남는다.
- **쉼 없이 이어지는 구간은 여전히 단어가 깨질 수 있다.** Apple 이 그 구간에서 run 사이 간격을 전부 0 으로 준다 — 쪼갤 근거가 데이터에 없다. 실측 3분에서 24초 동안 간격이 0 인 구간이 있었고, 거기서 hard 12초 상한에 걸려 2번 잘렸다(수정 전 14번). 대신 10초 넘는 긴 자막이 4개 생기는데, 말이 안 쉬는 구간에서 단어를 깨느냐 한 줄을 길게 두느냐의 선택이다. 되돌리려면 `hardSegmentDuration` 을 낮추면 된다.
- **Apple 인식은 실행마다 결과가 조금씩 다르다.** 같은 파일 같은 구간인데 "きれいですね" → "きりですね" → "霧ですね" 로 나왔다. 수십 자 수준의 차이는 노이즈로 보고 비교해야 한다.
- **WhisperKit 은 검토했고 도입하지 않았다.** argmax-oss-swift 로 같은 구간을 실측했다: 인식 품질은 서로 이기고 지는 관계였고(Apple 이 `バイブ`·`1本目`·`栗派か中派か` 를, Whisper 가 `今回のおもちゃって…`·`これちっちゃ`·`電マ` 를 맞혔다), 문자 수는 +13%, 속도는 16.5배속(180초를 11초), 모델 626MB. Whisper 쪽에는 반복 루프 할루시네이션(`楽しくなってます` 3회, `あー。` 10회)과 웃음소리 텍스트화가 있다. **번역 보완재로는 못 쓴다** — translate 태스크는 영어로만 번역하는데, 이 소재에서는 영어조차 안 나오고 일본어가 그대로 나왔다. 나중에 붙인다면 번역이 아니라 **인식 엔진 선택지**로, Apple 이 지원하지 않는 언어의 대안으로서다.
- **음악·반향이 심한 구간은 인식이 뭉개진다.** Apple 인식기의 한계로, 파이프라인 쪽에서 더 올릴 방법이 없다.
- **5.1 센터 채널 추출은 시도했다가 되돌렸다.** 채널 선택은 정확히 동작했지만(AAC 5.1은 인덱스 0, E-AC-3는 1) 품질 개선을 증명하지 못했다. 되살리려면 `AVAudioFormat`이 3채널 이상에서 채널 레이아웃 없이는 nil을 반환한다는 점을 기억할 것.
- **Claude 백엔드는 텍스트를 외부로 전송한다.** 앱의 "오디오는 전송되지 않는다"는 안내는 인식에 대해서만 사실이다.
